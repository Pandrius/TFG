# Servicio de IA - dependencias.

fastapi
uvicorn[standard]
python-multipart        # subida de archivos en FastAPI
markitdown[pdf,docx,xlsx]  # extraccion estructurada a Markdown (Microsoft)

# Extraccion de texto
pypdf                   # .pdf
python-docx             # .docx
openpyxl                # .xlsx
python-pptx             # .pptx
charset-normalizer      # deteccion de codificacion (.txt, .csv)

# Pruebas
pytest
reportlab               # genera PDFs de prueba

# --- Modelo real (Hito 9) ---
transformers>=4.40
torch>=2.2
scikit-learn==1.8.0
joblib>=1.3
numpy>=1.26
